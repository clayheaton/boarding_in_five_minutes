--! file: grid.lua

-- The Grid is the backbone structure to the Playfield. 
-- The movement of a Person is controlled along the Grid.
-- Hence, the grid implements methods that support pathfinding.

Grid = Object:extend()

function Grid:new()

end

function Grid:update(dt)

end

function Grid:draw()

end